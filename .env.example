# Base URL of the backend API (no trailing slash). Leave empty while mocking.
VITE_API_BASE_URL=

# When "true", src/services/* return local mock data instead of hitting the
# network. Set to "false" once the backend is available.
VITE_USE_MOCK_API=true
